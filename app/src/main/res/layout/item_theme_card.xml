<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="vertical" android:layout_margin="8dp">

    <FrameLayout
        android:id="@+id/themePreviewBox"
        android:layout_width="match_parent" android:layout_height="120dp"
        android:background="@drawable/bg_continue_card"
        android:clickable="true" android:focusable="true"
        android:foreground="?attr/selectableItemBackground">

        <TextView
            android:id="@+id/themePreviewSample"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Aa" android:textSize="30sp" android:textStyle="bold"/>

        <View
            android:id="@+id/themeAccentDot"
            android:layout_width="14dp" android:layout_height="14dp"
            android:layout_gravity="bottom|start" android:layout_margin="10dp"/>

        <ImageView
            android:id="@+id/themeActiveCheck"
            android:layout_width="22dp" android:layout_height="22dp"
            android:layout_gravity="top|end" android:layout_margin="8dp"
            android:src="@drawable/ic_star_filled"
            android:visibility="gone"/>

        <ImageButton
            android:id="@+id/themeOverflow"
            android:layout_width="28dp" android:layout_height="28dp"
            android:layout_gravity="top|start" android:layout_margin="6dp"
            android:src="@drawable/ic_more_vert" android:background="@drawable/bg_button_dark"
            android:padding="4dp" android:tint="#DDDDDD"
            android:contentDescription="Theme options"
            android:visibility="gone"/>

    </FrameLayout>

    <TextView
        android:id="@+id/themeCardName"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:textSize="12sp" android:textStyle="bold" android:textColor="#EEEEEE"
        android:maxLines="1" android:ellipsize="end"
        android:layout_marginTop="8dp"/>

    <TextView
        android:id="@+id/themeCardKind"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:textSize="10sp" android:textColor="#707070"
        android:layout_marginTop="2dp"/>

</LinearLayout>