<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="neunix.pageflow">

    <!-- Storage: needed below API 33 to read PDFs from file manager -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Keep screen on while reading -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- TTS engine communication -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- OpenGL ES 2.0 for page curl -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.PageFlow"
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:supportsRtl="true">

        <!-- pdfbox-android font cache — required by com.tom-roush:pdfbox-android -->
        <meta-data
            android:name="pdfbox.fontcache"
            android:value="${applicationId}"/>

        <!-- HOME SCREEN -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Accept PDFs opened from file managers / email / browser -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:mimeType="application/pdf"/>
            </intent-filter>

        </activity>

        <!-- PDF READER -->
        <activity
            android:name=".PdfActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:configChanges="orientation|screenSize|keyboardHidden"/>

    </application>

</manifest>